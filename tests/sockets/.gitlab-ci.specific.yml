image: docker:latest

stages:
  - sockets

'Job 1':
  stage: sockets
  services:
    - image:tag
    - docker:19-dind
  script:
    - echo 'Job 1'
    - echo "DOCKER_HOST=${DOCKER_HOST}"
    - timeout 5 docker system info
    - timeout 5 docker ps

'Job 3':
  stage: sockets
  script:
    - echo 'Job 2'
